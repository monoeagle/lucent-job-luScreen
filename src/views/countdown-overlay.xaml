<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LucentScreen Countdown"
        Width="170" Height="110"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border CornerRadius="12" Background="#CC1A1D26" BorderBrush="#FF00B7EB" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="TxtCount"
                       FontFamily="Segoe UI"
                       FontSize="48"
                       FontWeight="SemiBold"
                       Foreground="#FFE8ECF2"
                       HorizontalAlignment="Center"
                       Text="3"/>
            <TextBlock Text="ESC = Abbruch"
                       FontFamily="Segoe UI"
                       FontSize="11"
                       Foreground="#FFA8B0C2"
                       HorizontalAlignment="Center"
                       Margin="0,2,0,0"/>
        </StackPanel>
    </Border>
</Window>
